{
  "name": "@cloacina/ui-e2e",
  "version": "0.10.0",
  "private": true,
  "type": "module",
  "description": "Playwright tooling for the Cloacina web UI (the app itself is the Rust/Leptos crate in this directory — CLOACI-I-0141). This package exists ONLY for the e2e + visual suites.",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "e2e": "playwright test",
    "test:visual": "E2E_BASE_URL=http://localhost:8080 E2E_SERVER_URL=http://localhost:8080 E2E_API_KEY=clk_demo_bootstrap_key_0001 E2E_TENANT=public playwright test visual.spec.ts --project=chromium"
  },
  "devDependencies": {
    "@playwright/test": "1.49.1",
    "typescript": "5.7.2"
  }
}
