[package]
name = "python-packaged-graph-example"
version = "0.1.0"
interface = "cloacina-workflow-plugin"
interface_version = 1
extension = "cloacina"

[metadata]
package_type = ["computation_graph"]
graph_name = "market_maker"
language = "python"
description = "Python market maker computation graph package"
entry_module = "market_maker.graph"
reaction_mode = "when_any"
input_strategy = "latest"
