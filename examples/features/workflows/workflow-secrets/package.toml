[package]
name = "workflow-secrets-demo"
version = "0.1.0"
interface = "cloacina-workflow-plugin"
interface_version = 1
extension = "cloacina"

[metadata]
workflow_name = "notify_oncall"
language = "rust"
description = "Workflow secrets — declared, tenant-stored, resolved at execution, never persisted"
author = "Cloacina Demo Team"
